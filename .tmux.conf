# Minimal modular tmux loader
run-shell 'TMUX_CONF_DIR="${TMUX_CONF_DIR:-$HOME/dotfiles/tmux/conf.d}"; if [ -d "$TMUX_CONF_DIR" ]; then for f in "$TMUX_CONF_DIR"/*.conf; do [ -f "$f" ] && tmux source-file "$f"; done; else echo "tmux: TMUX_CONF_DIR not found: $TMUX_CONF_DIR" >&2; fi'
