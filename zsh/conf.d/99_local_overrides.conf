# ============================================================
# load local config (final override stage)
# ============================================================
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
