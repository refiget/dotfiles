# ============================================================
# local final overrides (optional)
# ============================================================
# Final machine-local compatibility hook.
# Note: this file may not run if 95_autostart_tmux.conf execs into tmux first.
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
