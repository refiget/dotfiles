# ============================================================
# 🚀 Auto-Start Tmux (Linux Server Only)
# ============================================================
if [[ -z "$TMUX" && "$(uname)" == "Linux" ]]; then
  exec tmux new-session -A -s main
fi
