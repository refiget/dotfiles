# ============================================
# 🚫 Trae terminal: 完全跳过 tmux 与复杂初始化
# ============================================
if [[ "$TERM_PROGRAM" == "Trae" ]]; then
  unset TMUX
  unset TMUX_PANE
  return 0
fi

# ============================================
# 🚀 Auto-Start tmux (Linux server only)
# ============================================
if [[ -z "$TMUX" && "$(uname)" == "Linux" ]]; then
  exec tmux new-session -A -s main
fi

