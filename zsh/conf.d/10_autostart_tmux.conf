# ============================================
# Auto-start tmux (interactive local usage)
# ============================================
if [[ -o interactive && -z "$TMUX" ]]; then
  exec tmux new-session -A -s main
fi
