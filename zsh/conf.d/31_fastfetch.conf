# ============================================================
# Fastfetch customization (disabled)
# ============================================================
# Bob requested reverting to the default fastfetch behavior.
# The previous Brian-logo + chafa cache pipeline has been removed.
#
# If you ever want to re-enable a custom logo workflow, reintroduce it here.
