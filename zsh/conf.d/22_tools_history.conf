# ============================================================
# History helpers: atuin
# ============================================================

if command -v atuin >/dev/null 2>&1; then
  eval "$(atuin init zsh --disable-up-arrow)"
fi
