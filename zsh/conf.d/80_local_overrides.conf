# ============================================================
# local pre-overrides (optional)
# ============================================================
# Reserved for machine-local tweaks that should run BEFORE 90/95 modules.
# If you need final authority, put overrides in ~/.zshrc.local (loaded by 99_local_overrides.conf).
