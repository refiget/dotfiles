# ============================================================
# PATH and tool environment
# ============================================================

# Import Homebrew env first, then normalize PATH in one place.
if command -v brew >/dev/null 2>&1; then
  eval "$(brew shellenv)"
fi

typeset -U path PATH

# Dotfiles scripts (exported for reuse in aliases/functions)
if [[ -n "${DOTFILES_DIR:-}" && -d "$DOTFILES_DIR/scripts" ]]; then
  export SCRIPTS_DIR="$DOTFILES_DIR/scripts"
elif [[ -d "$HOME/scripts" ]]; then
  export SCRIPTS_DIR="$HOME/scripts"
fi

path=(
  "$HOME/.local/bin"
  "$HOME/.opencode/bin"
  "$HOME/Library/pnpm"
  ${SCRIPTS_DIR:-}
  "/opt/homebrew/bin"
  "/opt/homebrew/sbin"
  "/usr/local/bin"
  "/usr/local/sbin"
  "/usr/bin"
  "/bin"
  "/usr/sbin"
  "/sbin"
  $path
)

export PNPM_HOME="$HOME/Library/pnpm"
