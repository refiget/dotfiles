# yazi

Yazi file manager configuration.

- `theme.toml` selects the active flavor.
- Flavors live under `flavors/`.

Note: theme tweaks may be reverted if they don’t behave reliably across terminal/tmux environments.
